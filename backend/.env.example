# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# InstantDB Configuration
INSTANTDB_APP_ID=your-instantdb-app-id
INSTANTDB_ADMIN_TOKEN=your-instantdb-admin-token

# Storage Mode: 'local' for testing (saves to backend/data/), 'instantdb' for production
# If not set, automatically uses 'local' if INSTANTDB_APP_ID is missing
STORAGE_MODE=local
