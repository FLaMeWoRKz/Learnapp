# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# InstantDB Configuration
INSTANTDB_APP_ID=your-instantdb-app-id
INSTANTDB_ADMIN_TOKEN=your-instantdb-admin-token

# Storage Mode: 'local' for testing (saves to backend/data/), 'instantdb' for production
# If not set, automatically uses 'local' if INSTANTDB_APP_ID is missing
STORAGE_MODE=local

# E-Mail (SMTP) – für Verifikation, Passwort-Reset etc.
# Gmail: Zwei-Faktor aktivieren, dann App-Passwort unter https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=deine-email@gmail.com
SMTP_PASS=dein-app-passwort-16-zeichen
MAIL_FROM=deine-email@gmail.com
APP_BASE_URL=http://localhost:5173

# Support-Kontakt (wird bei generischen Fehlermeldungen angezeigt)
SUPPORT_EMAIL=deine-support-email@example.com
